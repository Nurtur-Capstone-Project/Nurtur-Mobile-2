name: nurtur_app
description: A new Flutter project generated by assistant.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^5.0.0
  # State management
  provider: ^6.0.5
  # Networking
  dio: ^5.3.2
  # Dependency injection
  get_it: ^7.6.0
  # JSON serialization
  json_annotation: ^4.8.0
  # Local storage
  shared_preferences: ^2.1.1
  # Utilities
  connectivity_plus: ^4.0.1
  cached_network_image: ^3.2.3
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  json_serializable: ^6.8.0
  mockito: ^5.5.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
  # fonts: add font assets here when available (leave commented or add a valid list)
